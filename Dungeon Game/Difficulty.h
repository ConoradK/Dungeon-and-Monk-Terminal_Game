// ifndef checks if the identifier DIFFICULTY_H has not been defined yet
#ifndef DIFFICULTY_H
// ifndef block starts here

// define the identifier DIFFICULTY_H to prevent double inclusion of the header file
#define DIFFICULTY_H

// enum declaration for representing different difficulty levels
enum Difficulty {
    Easy = 0,     // Easy difficulty level, assigned value 0
    Medium = 1,   // Medium difficulty level, assigned value 1
    Hard = 2      // Hard difficulty level, assigned value 2
};

// endif closes the ifndef block
#endif // DIFFICULTY_H
// comment indicating the end of the ifndef block
