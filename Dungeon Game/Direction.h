// ifndef checks if the identifier DIRECTION_H has not been defined yet
#ifndef DIRECTION_H
// ifndef block starts here

// define the identifier DIRECTION_H to prevent double inclusion of the header file
#define DIRECTION_H

// enum declaration for representing different directions
enum Direction {
    North = 0,   // North direction, assigned value 0
    West = 1,    // West direction, assigned value 1
    East = 2,    // East direction, assigned value 2
    South = 3    // South direction, assigned value 3
};

// endif closes the ifndef block
#endif // DIRECTION_H
// comment indicating the end of the ifndef block
